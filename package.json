{
  "name": "lms-backend",
  "version": "1.0.0",
  "description": "Learning Management System Backend",
  "scripts": {
    "start": "cd backend-lms && php artisan serve --host=0.0.0.0 --port=$PORT",
    "build": "cd backend-lms && composer install --no-dev --optimize-autoloader",
    "postinstall": "cd backend-lms && composer install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
